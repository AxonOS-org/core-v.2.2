# AxonOS Core Dependencies
# Core protocol dependencies - only critical packages
# Security is CORE, not optional!

# --- Core Math & Signal Processing ---
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
mne>=1.6.0          # Standard for EEG/MEG

# --- Deep Learning (PyTorch 2.x is mandatory in 2026) ---
torch>=2.2.0

# --- SECURITY & PRIVACY (CORE - NOT OPTIONAL!) ---
# Cryptography is mandatory for "Private by design"
cryptography>=42.0.0
bcrypt>=4.1.0
pydantic>=2.6.0     # Strict validation for protocol schemas